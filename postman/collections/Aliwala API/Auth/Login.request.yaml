$kind: http-request
method: POST
url: "{{baseUrl}}/api/auth/login"
order: 1000
headers:
  - key: Content-Type
    value: application/json
auth:
  type: noauth
body:
  type: json
  content: |-
    {
      "email": "admin@example.com",
      "password": "password123"
    }
